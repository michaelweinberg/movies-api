spring:
  application:
    name: moviesv2
  jpa:
    hibernate:
      ddl-auto: none
    database: postgresql
  datasource:
    url: ${POSTGRES_URL}
    username: ${POSTGRES_USERNAME}
    password: ${POSTGRES_PASSWORD}
